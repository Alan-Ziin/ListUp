import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    input: {
        width: "100%",
        height: 52,
        fontSize: 14,
        borderRadius: 10,
        borderWidth: 1,
        padding: 12,
        borderColor:"rgb(56, 56, 72)",
    }
})